import { useEffect } from "react";

export function CardUserReq(userId) {
    useEffect(() => {
       userSerVice 
    })
  return (
    <li key={user.id}>
      <h3>{user.fullName}</h3>
      <p>Email: {user.email}</p>
      <p>Số điện thoại: {user.phone}</p>
      <p>CMND: {user.CMND}</p>
    </li>
  );
}
